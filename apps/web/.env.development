# Dev เท่านั้น — Next rewrites proxy WebSocket ไม่เสถียร เลยให้ browser ต่อ control-plane ตรง
# (cookie ไม่แยก port บน localhost เลยแนบไปด้วยได้ ส่วน Origin ต้องอยู่ใน ALLOWED_ORIGINS ของ control-plane)
NEXT_PUBLIC_WS_BASE=ws://localhost:8080
