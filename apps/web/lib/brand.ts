// ชื่อที่แสดงให้ user เห็นทุกที่ (header, sidebar, tab title, หน้า login) — ที่เดียว
// เพื่อไม่ให้เขียนต่างกันตามหน้า. "game-manager" ที่เหลืออยู่ในระบบเป็น identifier
// (ชื่อ repo/module/container/label/image) ไม่ใช่ display — ห้ามเปลี่ยนตามชื่อนี้
export const APP_NAME = "Game manager";
