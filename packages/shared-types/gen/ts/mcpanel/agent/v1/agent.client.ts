// @generated by protobuf-ts 2.11.1
// @generated from protobuf file "mcpanel/agent/v1/agent.proto" (package "mcpanel.agent.v1", syntax proto3)
// tslint:disable
import type { RpcTransport } from "@protobuf-ts/runtime-rpc";
import type { ServiceInfo } from "@protobuf-ts/runtime-rpc";
import { AgentService } from "./agent";
import { stackIntercept } from "@protobuf-ts/runtime-rpc";
import type { ControlMessage } from "./agent";
import type { AgentMessage } from "./agent";
import type { DuplexStreamingCall } from "@protobuf-ts/runtime-rpc";
import type { RpcOptions } from "@protobuf-ts/runtime-rpc";
/**
 * AgentService คือ RPC หลักที่ node-agent เปิด bidirectional stream ค้างไว้กับ
 * control-plane ตั้งแต่ boot จนกว่าจะ disconnect
 *
 * ช่องทางนี้ใช้เฉพาะข้อมูล realtime: heartbeat, console I/O, server status
 * ส่วน "คำสั่ง" (start/stop/create/delete) ต้องผ่าน NATS JetStream เสมอ (ดู job/v1)
 *
 * Auth: agent แนบ metadata `authorization: Bearer <node-token>` ตอนเปิด stream
 * control-plane resolve node id จาก token เอง — ห้ามเชื่อ node_id ที่ client ส่งมา
 *
 * @generated from protobuf service mcpanel.agent.v1.AgentService
 */
export interface IAgentServiceClient {
    /**
     * @generated from protobuf rpc: Connect
     */
    connect(options?: RpcOptions): DuplexStreamingCall<AgentMessage, ControlMessage>;
}
/**
 * AgentService คือ RPC หลักที่ node-agent เปิด bidirectional stream ค้างไว้กับ
 * control-plane ตั้งแต่ boot จนกว่าจะ disconnect
 *
 * ช่องทางนี้ใช้เฉพาะข้อมูล realtime: heartbeat, console I/O, server status
 * ส่วน "คำสั่ง" (start/stop/create/delete) ต้องผ่าน NATS JetStream เสมอ (ดู job/v1)
 *
 * Auth: agent แนบ metadata `authorization: Bearer <node-token>` ตอนเปิด stream
 * control-plane resolve node id จาก token เอง — ห้ามเชื่อ node_id ที่ client ส่งมา
 *
 * @generated from protobuf service mcpanel.agent.v1.AgentService
 */
export class AgentServiceClient implements IAgentServiceClient, ServiceInfo {
    typeName = AgentService.typeName;
    methods = AgentService.methods;
    options = AgentService.options;
    constructor(private readonly _transport: RpcTransport) {
    }
    /**
     * @generated from protobuf rpc: Connect
     */
    connect(options?: RpcOptions): DuplexStreamingCall<AgentMessage, ControlMessage> {
        const method = this.methods[0], opt = this._transport.mergeOptions(options);
        return stackIntercept<AgentMessage, ControlMessage>("duplex", this._transport, method, opt);
    }
}
