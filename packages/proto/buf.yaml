version: v2
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    # bidirectional stream ใช้ชื่อ AgentMessage/ControlMessage สื่อทิศทางชัดกว่า
    # ConnectRequest/ConnectResponse — จงใจยกเว้น
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
